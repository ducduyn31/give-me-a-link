---
'give-me-a-link': minor
---

Add a toolbar button as a second way to trigger the copy, so the extension works in browsers that don't deliver extension keyboard shortcuts — notably Arc, where the `Cmd+.`/`Ctrl+.` command never fires. The keyboard shortcut keeps working everywhere it did before.
